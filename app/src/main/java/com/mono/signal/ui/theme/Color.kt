package com.mono.signal.ui.theme

import androidx.compose.ui.graphics.Color

/** MONO Signal palette, lifted verbatim from the design mockup's CSS tokens. */
object MonoColors {
    // Surfaces
    val Void = Color(0xFF050507)
    val Ink = Color(0xFF0B0B10)
    val Onyx = Color(0xFF111118)
    val Graphite = Color(0xFF1A1A22)
    val Slate = Color(0xFF2A2A35)
    val Fog = Color(0xFF444452)

    // Foreground
    val Fg1 = Color(0xFFF4F4F8)
    val Fg2 = Color(0xFFC5C5D0)
    val Fg3 = Color(0xFF8888A0)
    val Fg4 = Color(0xFF555568)

    // Accents
    val Turquoise = Color(0xFF00E5D1)
    val TurquoiseGlow = Color(0xFF5CFFEC)
    val Crimson = Color(0xFFFF2E7E)
    val CrimsonGlow = Color(0xFFFF6AA8)
    val Violet = Color(0xFFC8B6FF)
    val Info = Color(0xFF5CD1FF)

    // Hairlines
    val BorderHairline = Color(0x0FFFFFFF) // rgba(255,255,255,.06)
    val BorderSoft = Color(0x1AFFFFFF)     // rgba(255,255,255,.10)
}
